#include<bits/stdc++.h>
using namespace std;

int calculateSum(int a, int b){
  cout<<a+b; 
}

int main (){
   int a;
   cout<<"Enter the value of a : "; 
    cin>>a; 
   int b; 
   cout<<"Enter the value of b : "; 
    cin>>b; 
   calculateSum(a, b); 

  return 0;
}